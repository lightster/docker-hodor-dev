postgres:
  image: postgres:9.3
  restart: always
  ports:
    - 5432
  volumes:
    - ./volumes/postgresql/var/lib/postgresql/data/pgdata:/var/lib/postgresql/data/pgdata
  environment:
    - PGDATA=/var/lib/postgresql/data/pgdata
    - POSTGRES_PASSWORD=postgres
